# JSON Diff ツール 設計書 INDEX

## 📚 ドキュメント一覧

| No. | ドキュメント | 概要 |
|-----|-------------|------|
| 01 | [要件定義書](./01-requirements.md) | 機能要件・非機能要件・ユースケース |
| 02 | [アーキテクチャ設計書](./02-architecture.md) | 全体構成・レイヤー設計・依存関係 |
| 03 | [ドメインモデル設計書](./03-domain-model.md) | エンティティ・値オブジェクト・ドメイン関数 |
| 04 | [関数型設計書](./04-functional-design.md) | 純粋関数・関数合成・副作用分離 |
| 05 | [API設計書](./05-api-design.md) | ポート定義・Worker API・ユースケース |
| 06 | [UI設計書](./06-ui-design.md) | デザインシステム・コンポーネント・アクセシビリティ |
| 07 | [テスト駆動開発設計書](./07-testing-strategy.md) | TDD原則・テスト戦略・E2Eテスト |

## 🎯 設計原則

### ドメイン駆動設計 (DDD)
- **ユビキタス言語**: 開発チーム全体で共通の用語を使用
- **境界づけられたコンテキスト**: Parsing / Comparing / Reporting
- **エンティティと値オブジェクト**: JsonDocument, DiffResult / JsonPath, DiffEntry

### 関数型プログラミング (FP)
- **純粋関数**: 差分計算・パース・フォーマットは全て純粋関数
- **イミュータブルデータ**: `readonly`修飾子で不変性を強制
- **関数合成**: `pipe`/`compose`によるパイプライン処理
- **Result型**: 例外を使わないエラーハンドリング

### 依存性逆転の法則 (DIP)
- **ポートとアダプター**: StoragePort, HttpPort, ExportPort
- **依存グラフ**: Domain → Application → Infrastructure
- **テスタビリティ**: モック可能なインターフェース設計

### テスト駆動開発 (TDD)
- **Red-Green-Refactor**: 失敗するテスト → 最小限の実装 → リファクタリング
- **テストピラミッド**: Unit Tests > Integration Tests > E2E Tests
- **テストファースト**: 実装前にテストを書く
- **カバレッジ目標**: 80%以上（branches, functions, lines, statements）

## 🏗️ ディレクトリ構造

```
src/
├── react-app/
│   ├── domain/           # ビジネスロジック（純粋）
│   │   ├── types/        # 型定義
│   │   │   └── __tests__/  # 型のユニットテスト
│   │   ├── entities/     # エンティティ
│   │   │   └── __tests__/  # エンティティテスト
│   │   ├── value-objects/# 値オブジェクト
│   │   │   └── __tests__/  # 値オブジェクトテスト
│   │   └── functions/    # ドメイン関数
│   │       └── __tests__/  # ドメイン関数テスト
│   │
│   ├── application/      # アプリケーションサービス
│   │   ├── ports/        # ポートインターフェース
│   │   ├── use-cases/    # ユースケース
│   │   │   └── __tests__/  # ユースケーステスト
│   │   └── state/        # 状態管理
│   │
│   ├── infrastructure/   # 外部依存の実装
│   │   ├── adapters/     # ポート実装
│   │   └── workers/      # Web Worker
│   │
│   ├── presentation/     # UI層
│   │   ├── components/   # React コンポーネント
│   │   │   └── __tests__/  # コンポーネントテスト
│   │   ├── hooks/        # カスタムフック
│   │   │   └── __tests__/  # フックテスト
│   │   └── styles/       # スタイル
│   │
│   ├── shared/           # 共有ユーティリティ
│   │
│   └── test/             # テスト共通設定
│       ├── setup.ts      # テストセットアップ
│       └── mocks/        # モック定義
│           └── handlers.ts
│
├── worker/               # Cloudflare Worker
│   ├── routes/           # APIルート
│   └── services/         # Workerサービス
│
└── e2e/                  # E2Eテスト
    ├── diff-flow.spec.ts
    └── accessibility.spec.ts
```

## 🔄 データフロー

```
User Input
    ↓
Presentation (React Component)
    ↓
Application (Use Case)
    ↓
Domain (Pure Functions) ← ここで差分計算
    ↓
Application (State Update)
    ↓
Presentation (Re-render)
```

## ✅ 実装チェックリスト

### Phase 1: コアドメイン
- [ ] 型定義 (json.ts, diff.ts, result.ts)
- [ ] 値オブジェクト (JsonPath, ValidationError)
- [ ] エンティティ (JsonDocument, DiffResult)
- [ ] ドメイン関数 (parser, differ, formatter, validator)

### Phase 2: アプリケーション層
- [ ] ポートインターフェース定義
- [ ] ユースケース実装
- [ ] 状態管理（Reducer）

### Phase 3: インフラ層
- [ ] LocalStorageAdapter
- [ ] FetchAdapter
- [ ] ExportAdapter
- [ ] Web Worker（差分計算用）

### Phase 4: Worker API
- [ ] 共有API (/api/share)
- [ ] JSONフェッチAPI (/api/fetch-json)
- [ ] KVストレージ連携

### Phase 5: プレゼンテーション層
- [ ] Atomsコンポーネント
- [ ] Moleculesコンポーネント
- [ ] Organismsコンポーネント
- [ ] メインレイアウト
- [ ] カスタムフック

### Phase 6: 品質保証
- [ ] ユニットテスト（ドメイン関数）
- [ ] 統合テスト（ユースケース）
- [ ] E2Eテスト（主要フロー）
- [ ] アクセシビリティ検証

## 📝 更新履歴

| 日付 | バージョン | 変更内容 |
|------|-----------|----------|
| 2024-XX-XX | 1.0.0 | 初版作成 |

